from app import app

# db = SQLAlchemy()
# app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///forrest.db'
# db.init_app(app)

# forrest = Flask(__name__)
# forrest.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///forrest.db'
# db = SQLAlchemy(forrest)


# basedir = os.path.abspath(os.path.dirname(__file__))

# app = Flask(__name__)
# app.config['SQLALCHEMY_DATABASE_URI'] =\
#         'sqlite:///' + os.path.join(basedir, 'database.db')
# app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False

# db = SQLAlchemy(app)

# if __name__ == "__main__":
#     app.run(debug=True)

# db.init_app(app)